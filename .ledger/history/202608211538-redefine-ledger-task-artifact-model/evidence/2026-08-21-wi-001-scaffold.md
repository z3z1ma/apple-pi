Status: recorded
Created: 2026-08-21
Updated: 2026-08-21

# WI-001 scaffold behavior

## Purpose

Verify AC-001 and AC-002: `ledger_add` creates exactly the approved root files/directories and the intent-focused `task.md` plus top-level retrospective scaffold, without changing archive behavior.

## Source State

- Base revision: `8706e4d302abdbf0f6dd334f4c73c51827902ada`.
- Changed paths: `extensions/ledger.ts`, `tests/ledger-add.test.ts`.
- Runtime: Node.js project dependencies through local `npx vitest` and Biome.
- Raw outputs: `evidence/.storage/wi-001/red.txt` and `evidence/.storage/wi-001/green.txt`.

## Procedure

1. Changed the production-boundary test first to assert the exact bundle members and literal contents of `task.md` and `retrospective.md`.
2. Ran `npx vitest run tests/ledger-add.test.ts` before production changes.
3. Replaced the scaffold directory list and root templates in `extensions/ledger.ts`, then created `retrospective.md` inside the existing rollback boundary.
4. Ran `npx vitest run tests/ledger-add.test.ts tests/ledger-close.test.ts`.
5. Ran `npx biome format extensions/ledger.ts tests/ledger-add.test.ts`, `npx biome lint extensions/ledger.ts tests/ledger-add.test.ts`, and `git diff --check -- extensions/ledger.ts tests/ledger-add.test.ts`.
6. Inspected the bounded diff for the two changed paths.

## Observations

- RED exited 1 with one failing test. The failure showed the old `knowledge/` and `skills/` members and missing `retrospective.md`, directly falsifying the new scaffold contract. The other four add tests passed.
- GREEN passed 11/11 tests across `tests/ledger-add.test.ts` and `tests/ledger-close.test.ts`.
- Focused Biome formatting and lint checks passed without fixes.
- `git diff --check` passed.
- The implementation preserves `AddedLedgerTask`, index behavior, collision cleanup, dependency handling, and `ledger_close`; only scaffold membership and root contents changed.
- Independent review returned specification `approved` and quality `approved`, with no material blockers. `OBS-WI001-01` identified only stale plan bookkeeping; it was addressed by setting WI-001 to `complete` and linking this evidence note.
- The verifier noted that unchanged whole-bundle `renameSync` plus the passing close suite makes explicit post-archive retrospective assertion a low-risk coverage limit rather than a defect.

## Limits

The focused tests prove the exact scaffold and existing close-suite assertions only. They do not yet prove injected prompt, documentation, lifecycle-skill, package-loader, or package-tarball consistency; those belong to WI-002 through WI-005.
