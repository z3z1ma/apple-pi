Status: recorded
Created: 2026-08-21
Updated: 2026-08-21

# WI-002 injected and durable Ledger contract

## Purpose

Verify AC-003 and the documentation portion of AC-004: the injected Ledger contract, durable documentation, repository guide, and public catalog teach the approved artifact owners and complete terminal predicate.

## Source State

- Base revision: `8706e4d302abdbf0f6dd334f4c73c51827902ada`.
- Changed paths: `components/shared/src/ledger-system-prompt.ts`, `tests/ledger-prompt-integration.test.ts`, `docs/ledger.md`, `AGENTS.md`, and `README.md`.
- Runtime: local Vitest and Biome dependencies.
- Raw outputs: `evidence/.storage/wi-002/red.txt`, `green-prompt.txt`, `green.txt`, `stale-contract-search.txt`, `format.txt`, `lint.txt`, `diff-check.txt`, and `check-exits.txt`.

## Procedure

1. Added production-prompt assertions before changing the injected contract. The assertions cover every artifact owner, all supporting-record statuses, assumption routing, research/evidence non-duplication, retrospective ownership, every material terminal-predicate clause, and removed task-local directories/destinations.
2. Ran `npx vitest run tests/ledger-prompt-integration.test.ts` and observed RED.
3. Replaced the injected Ledger contract with the approved ontology while preserving the prompt tag and idempotent append function.
4. Rewrote `docs/ledger.md` in place and aligned `AGENTS.md` plus the README catalog.
5. Re-ran the focused prompt suite.
6. Ran `npx biome format components/shared/src/ledger-system-prompt.ts tests/ledger-prompt-integration.test.ts docs/ledger.md README.md`.
7. Ran `npx biome lint components/shared/src/ledger-system-prompt.ts tests/ledger-prompt-integration.test.ts`.
8. Ran `git diff --check -- components/shared/src/ledger-system-prompt.ts tests/ledger-prompt-integration.test.ts docs/ledger.md AGENTS.md README.md`.
9. Searched the changed contract surfaces for `knowledge/`, the task-local skill path, the former routine-evidence sentence, and the former task-status/evidence sentence.

## Observations

- RED exited 1 with one failing test and eight passing tests. The failure showed the injected prompt still lacked the new `task.md` intent/acceptance contract and printed the old bundle/task ontology.
- GREEN passed 9/9 prompt-integration tests after the contract rewrite.
- The focused Biome format command exited 0 after checking the two supported source/test files; Markdown inputs require no formatter output under this command.
- The focused Biome lint command exited 0 after checking the two linted source/test files.
- The focused `git diff --check` command exited 0 across all five changed paths.
- The stale-contract search exited 1 with an empty output file, meaning none of the four forbidden old statements remained in the changed contract surfaces.
- `git diff --check` passed.
- `docs/ledger.md` now defines one current format and explicitly leaves archived bundles historical without adding migration or compatibility behavior.
- Independent review approved specification compliance. Its quality gate requested exact check provenance and current plan bookkeeping; the raw outputs and commands above address the provenance gap, and the active plan now links this note and marks WI-002 complete.

## Limits

These observations prove prompt construction/idempotence and focused contract consistency. They do not pressure-test every lifecycle skill or prove final package inclusion; WI-003 through WI-005 own those checks.
